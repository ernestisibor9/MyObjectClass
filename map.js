
// Map method for looping through an aray
const color = ['red', 'yellow', 'blue', 'green', 'white'];

      color.map(function(elements){
        console.log(`These are the colors @ ${elements}`);
      })